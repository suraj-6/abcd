{
  "name": "multi-engine-resume-builder",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "start:server": "cd server && npm start",
    "dev:client": "cd client && python3 -m http.server 5173",
    "start": "echo 'Run npm run dev:server and npm run dev:client in separate terminals'"
  }
}
